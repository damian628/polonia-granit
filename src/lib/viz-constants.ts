/**
 * Dzienny limit wizualizacji kuchni.
 * `0` = bez limitu (faza testów). Przed publikacją strony ustawić z powrotem na `2`.
 */
export const VIZ_DAILY_LIMIT = 0;

export const VIZ_LIMIT_ENABLED = VIZ_DAILY_LIMIT > 0;
